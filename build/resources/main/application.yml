server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: utf-8
      enabled: true


logging:
  level:
    org.springframework.security: TRACE

spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

  profiles:
    include: oauth

  ## Email Send Configuration_SMTP
  mail:
    host: smtp.gmail.com
    port: 587
    username: 'godgong2@gmail.com'
    password: 'godgong1234'
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  redis:
    host: localhost
    port: 6379


  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pillgood?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: mcys1309


  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
    hibernate:
      ddl-auto: create
    show-sql: true
    generate-ddl: true
    properties:
      hibernate.format_sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Asia/Seoul

app:
  cors:
    allowedOrigins: http://localhost:3000

gg:
  app:
    jwtExpirationMs: '604800000'
    jwtRefreshExpirationInMs: 86400000
    jwtSecret: jwtsigntutorialasdfasdfasdfasdfasdf

firebase:
  sdk:
    path: '/firebase/pillgood-sdk.json'









